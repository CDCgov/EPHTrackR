############################################################
##  Update the content area, indicator and measure data   ##
############################################################

.onAttach <- function(libname, pkgname) {

  packageStartupMessage("Run `update_measures()` to update the stored list of content areas, indicators, and measures.")
  
}
